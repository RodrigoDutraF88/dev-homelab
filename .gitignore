.env

# Local agent context — not part of the public learning log
CLAUDE.md
.claude/

# Self-signed dev certificates — regenerate with `make certs`, never commit a private key
infrastructure/traefik/certs/

# Backups, if BACKUP_DIR is ever pointed inside the repo
backups/
